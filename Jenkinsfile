pipeline {
    agent any
    stages {
        stage('Show workspace') {
            steps {
                sh 'ls -la'
            }
	}
	stage('Example') {
            steps {
                echo 'Hello World'
            }
	
        }
    }
}
